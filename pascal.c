#include <stdio.h>
int main()
{
	int i,j, val,rows;

	printf ("Enter the no. of rows");

	scanf("%d", &rows);

	for (i=0;i<rows; i++)



	for (j=0;j<rows-i;j++)

	printf("");

	val/=1;

	for(j=0;j<=1jj++);

{

	printf (%ud, val);

	val=val* (i-j)/Ej+i);

	print("\n");
}